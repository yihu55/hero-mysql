<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>mysql</title>
</head>
<body>
    <h1>Hero</h1>
       name:<%= hero.name%>
       age:<%= hero.age%>
 
    <form  method="POST">
    <select name="location">
        <% locations.forEach(location=>{%>
            
            <option value="<%= location.id%>"> <%= location.name%> i <%= location.region%> </option>
        <%})%>
       
    </select>

    <input type="submit" value="Save">
    </form>
</body>
</html>